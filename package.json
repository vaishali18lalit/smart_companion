{
  "name": "smart-knowledge-companion",
  "version": "1.0.0",
  "description": "AI-powered learning companion with real-time content",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && node server-openrouter.js",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm start\"",
    "install-all": "cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": ["ai", "learning", "knowledge", "education", "react", "nodejs"],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}